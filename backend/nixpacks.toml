# Nixpacks configuration for Lobster Report Backend

[phases.setup]
nixPkgs = ["python311", "postgresql"]
commands = [
  "pip install -r requirements.txt"
]

[phases.build]
commands = []

[start]
cmd = "./start.sh"
