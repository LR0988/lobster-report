{
  "name": "lobster-report",
  "version": "1.0.0",
  "description": "Lobster Report - Information Aggregation System",
  "scripts": {
    "start": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "dev": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
  },
  "engines": {
    "node": ">=18"
  }
}
